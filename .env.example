# RPC URL for the Ethereum network
RPC_URL=https://eth-mainnet.g.alchemy.com/v2/your-api-key

# Comma-separated list of private keys (no spaces after commas)
PRIVATE_KEYS=0x123...,0x456...,0x789...

# Comma-separated list of initial balances in ETH (no spaces after commas)
INITIAL_BALANCES=1.5,2.0,1.0

# Number of addresses to activate per wallet manager
ADDRESS_COUNT=5

# Optional: Log level (trace, debug, info, warn, error)
RUST_LOG=info 